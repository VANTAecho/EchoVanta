import os
import glob

def discover_modules(base_path="."):
    """
    Discover Python modules inside collectors/, detectors/, and responders/ folders.
    
    Args:
        base_path (str): Root path of the repo or project.

    Returns:
    """
